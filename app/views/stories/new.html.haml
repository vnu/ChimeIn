.chimeform
  = form_for(Story.new, :html => { :role => 'form'}) do |f|
    .form-group
      = f.label 'What would you like to share about?'
      = f.text_field :title, placeholder: 'Domestic Violence, Bullying etc.,', :autofocus => true, class: 'form-control'
    .form-group
      = f.label 'What are you looking for?'
      = f.text_field :looking_for, placeholder: 'advice.,', value: '', :autofocus => true, class: 'form-control'
    .form-group
      = f.label 'Tell us your story'
      = f.text_area(:content, size: "30x10")
    .checkbox{:style => "width:150px"}
      %label
        = f.check_box :anonymous
        Keep me anonymous
    = f.submit 'Share with community', :class => 'button right'
    %a.button.left{href: '#'}'Get connected with an expert'


